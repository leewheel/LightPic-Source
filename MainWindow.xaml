<Window x:Class="ZImageLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        x:Name="MainWindowObj"
        Title="Z-Image LowVram Edition" Height="800" Width="1200"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Black"
        BorderThickness="3"
        BorderBrush="#9932CC"
        ResizeMode="CanResizeWithGrip"
        Loaded="MainWindow_Loaded"
        SizeChanged="MainWindow_SizeChanged"
        Closing="MainWindow_Closing">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas, Courier New"/>
            <Setter Property="Foreground" Value="#00FF00"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#00FFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#00FF00"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#003300"/>
                    <Setter Property="Foreground" Value="#CCFFCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="120"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ClipToBounds="True" MouseLeftButtonDown="TopBar_MouseLeftButtonDown">
            <Rectangle Fill="#050505"/>
            <Canvas x:Name="StarCanvas" Height="80" VerticalAlignment="Top" IsHitTestVisible="False"/>
            <Canvas x:Name="NoiseCanvas" Opacity="0.15"/>
            <Line X1="0" Y1="79" X2="1200" Y2="79" Stroke="#00FF00" StrokeThickness="1" Opacity="0.5"/>
            <Line X1="0" Y1="0" X2="1200" Y2="0" Stroke="#00FF00" StrokeThickness="1" Opacity="0.5"/>
            <Canvas x:Name="WaveCanvas" ClipToBounds="True"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0" Background="Transparent">
                <Button Content="🗑️" Width="40" Height="25" Click="ClearLogs_Click" BorderThickness="0" ToolTip="清理旧日志文件"/>
                <Button Content="📂" Width="40" Height="25" Click="OpenFolder_Click" BorderThickness="0" ToolTip="打开今日输出目录"/>
                <Button Content="🖼️" Width="40" Height="25" Click="OpenImageBrowser_Click" BorderThickness="0" ToolTip="打开图片浏览器"/>
                <Button Content="─" Width="40" Height="25" Click="Minimize_Click" BorderThickness="0"/>
                <Button Content="☐" Width="40" Height="25" Click="Maximize_Click" BorderThickness="0"/>
                <Button x:Name="CloseButton" Content="✕" Width="40" Height="25" Click="Close_Click" BorderThickness="0"/>
            </StackPanel>

            <Canvas x:Name="ClockCanvas" HorizontalAlignment="Left" VerticalAlignment="Top" Width="220" Height="50" Margin="20,50,0,0" IsHitTestVisible="False"/>

            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,15,0,0" Orientation="Horizontal">
                <Button x:Name="WebUIButton" Content="WebUI" Height="28" Width="80" Click="LaunchWebUI_Click"
                        BorderThickness="1" Background="#002200" Foreground="#00FFFF"
                        ToolTip="启动 Z-Image WebUI"/>
                <Button x:Name="ComfyUIButton" Content="ComfyUI" Height="28" Width="80" Click="LaunchComfyUI_Click"
                        Margin="8,0,0,0"
                        BorderThickness="1" Background="#002200" Foreground="#00FFFF"
                        ToolTip="启动 ComfyUI"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" BorderBrush="#00FF00" BorderThickness="1" Margin="0,0,0,1">
            <wv2:WebView2 x:Name="MainWebView" DefaultBackgroundColor="#000000"/>
        </Border>

        <Grid Grid.Row="2" Background="#001100">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,5,0,0">
                <TextBlock Text="SYSTEM LOG"
                   FontSize="10"
                   Foreground="#008800"
                   MouseLeftButtonUp="SystemLog_Click"
                   Cursor="Hand"
                   VerticalAlignment="Center"/>
                <Ellipse x:Name="WebUIIndicator" Width="8" Height="8" Margin="5,0,0,0" Fill="Red" VerticalAlignment="Center" ToolTip="WebUI Python图形设置"/>
                <Ellipse x:Name="ComfyUIIndicator" Width="8" Height="8" Margin="3,0,0,0" Fill="Red" VerticalAlignment="Center" ToolTip="ComfyUI Python图形设置"/>
            </StackPanel>

            <TextBox x:Name="LogView"
             IsReadOnly="True"
             Background="Transparent"
             Foreground="#00FF00"
             BorderThickness="0"
             FontFamily="Segoe UI Symbol, Lucida Console, Consolas, Courier New"
             FontSize="11"
             VerticalScrollBarVisibility="Auto"
             Padding="5,20,5,5"
             TextWrapping="NoWrap"/>
            <Line X1="0" Y1="0" X2="1200" Y2="0" Stroke="#00FF00" StrokeThickness="1" Opacity="0.5"/>
        </Grid>
    </Grid>
</Window>
